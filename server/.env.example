# 서버 설정
PORT=5000
NODE_ENV=development

# MongoDB 설정
MONGODB_ALTAS_URL=mongodb+srv://your_username:your_password@cluster0.xxxxx.mongodb.net/your_database_name

# JWT 설정
# 보안을 위해 반드시 안전한 랜덤 키로 변경하세요!
# 생성 방법: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_KEY_MINIMUM_64_CHARACTERS
JWT_EXPIRE=30d

# 기타 설정
BCRYPT_SALT_ROUNDS=12

# CORS 설정 (프론트엔드 URL)
CLIENT_URL=http://localhost:3000

# 포트원(Portone) 결제 설정
IAMPORT_API_KEY=your_iamport_api_key_here
IAMPORT_API_SECRET=your_iamport_api_secret_here

# 보안 주의사항:
# 1. JWT_SECRET은 반드시 안전한 랜덤 키로 변경하세요
# 2. 프로덕션 환경에서는 NODE_ENV=production으로 설정하세요
# 3. 실제 MongoDB URI와 강력한 비밀번호를 사용하세요
# 4. 이 파일을 복사하여 .env 파일을 만들고 실제 값으로 변경하세요